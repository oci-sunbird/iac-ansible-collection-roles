apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingressroutehello
  namespace: sample
spec:
  entryPoints:
    - web
  routes:
  - match: Host(`hello.{{domain}}`) && PathPrefix(`/`)
    kind: Rule
    priority: 1
    services:
    - name: hello
      namespace: sample
      port: 5000
